/* eslint-disable @typescript-eslint/no-empty-function */
import { ReadFile, WriteFile } from './types';

// TODO implements
export default {
  readFile: (() => {}) as unknown as ReadFile,
  writeFile: (() => {}) as unknown as WriteFile,
};
